put:
  tags:
  - BBs
  summary: Add a recipient to certian message
  description: |
    Add a recipient to certian message
  security:
    - bearerAuth: []
  parameters:
    - name: id
      in: query
      description: id
      required: true
      style: simple
      explode: false
      schema:
        type: string
    - name: read
      in: query
      description: read
      style: simple
      explode: false
      schema:
        type: boolean
    - name: mute
      in: query
      description: mute
      style: simple
      explode: false
      schema:
        type: boolean      
  responses:
    200:
      description: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error  