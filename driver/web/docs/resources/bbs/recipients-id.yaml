post:
  tags:
  - BBs
  summary: Create a recipient
  description: |
    Create a recipient
  security:
    - bearerAuth: []
  parameters:
    - name: message-id
      in: path
      description: message-id
      required: true
      style: simple
      explode: false
      schema:
        type: string
    - name: read
      in: query
      description: read
      required: true
      style: simple
      explode: false
      schema:
        type: boolean
    - name: mute
      in: query
      description: mute
      required: true
      style: simple
      explode: false
      schema:
        type: boolean      
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../schemas/application/MessageRecipients.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error
delete:
  tags:
  - BBs
  summary: Remove a recipient to certian message
  description: |
    Remove a recipient to certian message
  security:
    - bearerAuth: []
  parameters:
    - name: id
      in: path
      description: id
      required: true
      style: simple
      explode: false
      schema:
        type: string
  responses:
    200:
      description: Success
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error
            