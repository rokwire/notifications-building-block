put:
  tags:
  - Client
  summary: Update message
  description: |
    Update message
  security:
    - bearerAuth: []
  requestBody:
    description: "body json of the read boolean"
    content:
      application/json:
        schema:
          $ref: "../../../schemas/apis/message-read/request/Request.yaml" 
    required: true    
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../../schemas/application/Message.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error
